import { CorefAnnotatorValueListDirective } from './coref-annotator-value-list.directive';

describe('CorefAnnotatorValueListDirective', () => {
  it('should create an instance', () => {
    const directive = new CorefAnnotatorValueListDirective();
    expect(directive).toBeTruthy();
  });
});
