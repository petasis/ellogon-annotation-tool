import { DomHelpersDirective } from './dom-helpers.directive';

describe('DomHelpersDirective', () => {
  it('should create an instance', () => {
    const directive = new DomHelpersDirective();
    expect(directive).toBeTruthy();
  });
});
