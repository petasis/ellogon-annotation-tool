import { ButtonAnnotatorValueListDirective } from './button-annotator-value-list.directive';

describe('ButtonAnnotatorValueListDirective', () => {
  it('should create an instance', () => {
    const directive = new ButtonAnnotatorValueListDirective();
    expect(directive).toBeTruthy();
  });
});
