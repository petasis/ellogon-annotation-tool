import { FoundInCollectionDirective } from './found-in-collection.directive';

describe('FoundInCollectionDirective', () => {
  it('should create an instance', () => {
    const directive = new FoundInCollectionDirective();
    expect(directive).toBeTruthy();
  });
});
